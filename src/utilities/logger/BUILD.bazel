package(default_visibility = ["//visibility:public"])

load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "logger",
    srcs = ["logger.cpp"],
    hdrs = ["logger.h"],
    deps = [
        "@grpc//:grpc++",
        "@protobuf//:protobuf",
    ],
)
