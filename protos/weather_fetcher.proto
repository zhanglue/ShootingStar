syntax = "proto3";

package weather_flow;

import "protos/weather_data.proto";

service WeatherFetcher {
  rpc GetWeather (GetWeatherRequest) returns (GetWeatherResponse);
}

message GetWeatherRequest {
  string city = 1;
}

message GetWeatherResponse {
  WeatherData data = 1;
}
